# Claude Code Web 使用教程

## 项目简介

将 Claude Code CLI 包装为 Web 应用，通过浏览器在手机、电脑等多端与 Claude Code 聊天，
并可指挥 Claude Code Agent 完成编码任务。
  
---

## 一、安装与启动

### 1. 安装依赖（首次运行）

```bash
cd /usr/code/SkillFrame/web-claude
npm install
```

> **注意**：`node-pty` 是原生模块，需要 gcc/python3。如安装失败：
> ```bash
> apt-get install -y build-essential python3
> npm install
> ```

### 2. 使用管理脚本（推荐）

项目提供 `server.sh` 一键管理脚本，覆盖启动、停止、端口配置、防火墙等所有操作：

```bash
./server.sh start          # 启动服务（默认端口 3000）
./server.sh start 8080     # 启动并指定端口
./server.sh stop           # 停止服务
./server.sh restart        # 重启服务
./server.sh status         # 查看运行状态（含访问地址、资源占用）
./server.sh logs           # 查看日志
./server.sh logs -f        # 实时跟踪日志
./server.sh url            # 显示所有访问地址
```

### 3. 手动启动（不使用管理脚本）

```bash
# 前台运行
node server.js

# 后台运行
PORT=8080 nohup node server.js >> server.log 2>&1 &
```

### 3. 访问地址

- 本机：`http://localhost:3000`
- 局域网：`http://服务器IP:3000`
- 公网：建议配置 Nginx 反代 + HTTPS（见下方）

---

## 二、用户管理（仅限终端操作）

用户管理**只能**在服务器终端通过 `manage_users.sh` 完成，Web 界面无注册入口。

```bash
# 切换到项目目录
cd /usr/code/SkillFrame/web-claude

# 添加用户
./manage_users.sh add <用户名> <密码> [工作目录]

# 示例：添加用户，工作目录为 SkillFrame
./manage_users.sh add alice mypassword /usr/code/SkillFrame

# 查看所有用户
./manage_users.sh list

# 修改密码
./manage_users.sh passwd alice newpassword

# 设置工作目录（Claude 启动时所在目录）
./manage_users.sh workdir alice /other/project

# 删除用户
./manage_users.sh remove alice
```

### 当前已有用户

| 用户名 | 工作目录 |
|--------|---------|
| lidongcheng | /usr/code/SkillFrame/ |

---

## 三、Web 界面操作说明

### 登录

打开浏览器访问服务地址，输入用户名和密码登录。
Token 有效期 24 小时，过期后自动跳转登录页。

### 聊天界面布局

```
┌─────────────────────────────────────────────────┐
│  Claude Code    ● 已连接  用户名   [文件] [退出] │  ← 顶栏
├─────────────────────────────────────────────────┤
│                                                 │
│  Claude  ← Claude 的输出（彩色终端格式）         │
│  ┌──────────────────────────────────┐           │
│  │ Hello! How can I help you today? │           │
│  └──────────────────────────────────┘           │
│                                          你的消息 │
│                        ┌─────────────────────┐  │
│                        │ 帮我写一个排序算法   │  │
│                        └─────────────────────┘  │
├─────────────────────────────────────────────────┤
│  [↑][↓][↵ 确认][Tab][Esc][/clear][⊘ 中断][⏏ 退出] │  ← 快捷键工具栏
├─────────────────────────────────────────────────┤
│  [ 输入消息…                        ] [ 发送 ]  │  ← 输入框
└─────────────────────────────────────────────────┘
```

### 发送消息

- **输入文字后按 Enter**：发送消息给 Claude
- **Shift + Enter**：换行（不发送）
- **点击发送按钮**：同 Enter

### 工具栏按钮

当 Claude 显示选项菜单、等待确认时，使用工具栏按钮操作：

| 按钮 | 作用 | 对应键盘操作 |
|------|------|------------|
| ↑ | 上移光标 / 选择上一项 | 方向键上 |
| ↓ | 下移光标 / 选择下一项 | 方向键下 |
| ↵ 确认 | 确认当前选项 / 继续 | Enter |
| Tab | 自动补全 | Tab |
| Esc | 取消当前操作 | Esc |
| /clear | 清屏 | Ctrl+L |
| ⊘ 中断 | 中断 Claude 当前操作 | Ctrl+C |
| ⏏ 退出 | 退出 Claude（返回 bash）| Ctrl+D |

### 文件浏览器

点击顶栏 **[文件]** 按钮打开文件浏览器：
- 点击文件夹：进入该目录
- 点击文件：将文件路径插入输入框（方便告诉 Claude 操作哪个文件）
- 点击 ↩ 上级目录：返回上一层

### Claude 选项菜单操作示例

当 Claude 提示如下菜单：
```
❯ Yes - 允许此操作
  No  - 拒绝
  Stop - 停止
```
使用 **↑↓** 选择，按 **↵ 确认** 执行。

---

## 四、会话机制说明

- 每次登录启动独立的 Claude Code 进程（PTY）
- **刷新页面不会断开会话**，历史输出自动回放
- 同一账号多个标签页共享同一个 Claude 进程
- 30 分钟无操作后，后台自动清理空闲进程
- Token 过期（24h）后重新登录，启动新的 Claude 进程

---

## 五、服务器管理（server.sh）

### 端口管理

```bash
./server.sh port              # 查看当前端口
./server.sh port 8080         # 更改端口（服务运行中会自动重启）
```

端口配置持久保存在 `.server.conf`，重启后自动生效。

### 防火墙管理

```bash
sudo ./server.sh firewall status          # 查看防火墙及规则
sudo ./server.sh firewall open            # 开放当前服务端口
sudo ./server.sh firewall open 8080       # 开放指定端口
sudo ./server.sh firewall close 8080      # 关闭指定端口
sudo ./server.sh firewall enable          # 启用防火墙（自动保留 SSH 端口）
sudo ./server.sh firewall disable         # 禁用防火墙
```

### 开机自启（systemd）

```bash
sudo ./server.sh install-service      # 安装为 systemd 服务，开机自启
sudo ./server.sh uninstall-service    # 卸载

# 安装后也可用 systemctl 管理
sudo systemctl start   claude-code-web
sudo systemctl stop    claude-code-web
sudo systemctl status  claude-code-web
sudo journalctl -u claude-code-web -f   # 查看日志
```

### 查看状态示例

```
./server.sh status

═══ Claude Code Web 服务状态 ═══

  状态      ● 运行中
  PID       12345
  端口      3000
  资源      CPU 0.3%  MEM 1.2%  运行时间 02:15:30
  活跃连接  2 个

访问地址：
  本机      http://localhost:3000
  局域网    http://172.31.4.82:3000

  防火墙    端口 3000 已开放
  开机自启  已启用（systemd）
```

---

## 六、生产环境配置（可选）

### Nginx 反代 + HTTPS

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # WebSocket 支持（必须）
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }
}
```

### 防火墙

```bash
# 只开放 443（HTTPS），不直接暴露 3000 端口
ufw allow 443
ufw deny 3000
```

---

## 六、项目文件结构

```
web-claude/
├── server.js           主服务（Express + WebSocket + PTY）
├── admin_helper.js     用户管理逻辑（供 bash 脚本调用）
├── manage_users.sh     用户管理终端脚本
├── server.sh           服务器管理脚本（启停/端口/防火墙/自启）
├── package.json        依赖配置
├── users.json          用户数据（bcrypt 加密密码，勿泄露）
├── .jwt_secret         JWT 签名密钥（自动生成，勿泄露）
├── .server.conf        端口等运行配置（自动生成）
├── server.log          运行日志
└── public/
    ├── index.html      登录页
    └── chat.html       聊天主界面
```

---

## 七、常见问题

**Q: 提示 "无法启动 Claude 进程"**
确认 `claude` 命令在 PATH 中：
```bash
which claude
# 如果没有，先完成 Claude Code 的认证
claude
```

**Q: 手机上字体太小**
点击顶栏 **A+** 按钮放大字体（可调范围 12px–20px，设置自动保存）。

**Q: 手机端点击发送需要点两次**
已修复。原因是第一次点击时软键盘收起导致按钮位移，浏览器误判为未命中。现在一次点击即可发送。

**Q: 手机端触摸聊天区域弹出软键盘**
已修复。触摸终端滚动区域不再触发软键盘弹出。

**Q: 手机端点击工具栏按钮（↑↓↵ 等）弹出软键盘**
已修复。工具栏导航按钮不再强制 focus 输入框，软键盘保持收起状态，方便查看 Claude 的选项菜单。

**Q: 手机端软键盘弹出/收起时聊天内容滚回顶部**
已修复。现在使用 ResizeObserver 精确检测容器尺寸变化（CSS 布局完成后触发），配合 requestAnimationFrame 确保 xterm 渲染完毕后再恢复滚动位置，不再出现跳位。

**Q: 断线后无法重连**
检查服务是否仍在运行：`ps aux | grep node`

**Q: 忘记密码**
在服务器终端重置：`./manage_users.sh passwd <用户名> <新密码>`

**Q: node-pty 编译失败**
```bash
apt-get install -y build-essential python3 libssl-dev
npm install --build-from-source
```

TODO:服务已拉起，但是和claude聊天没有能显示出聊天框里的字体，聊天框外的”claude“提示字体也太小，需要改变字体大小功能。请你写测试用例，主动进行测试，并修正优化存在的问题
支持聊天视图和文件视图切换，支持阅读.MD文件和代码文件等功能
